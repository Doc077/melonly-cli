import { BroadcastChannel, ChannelInterface } from '@melonly/core'

@BroadcastChannel('{{ name }}/:id')
export class {{ className }} implements ChannelInterface {
    public userAuthorized(): boolean {
        return true
    }
}
